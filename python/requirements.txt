# GPU inference stack for the marketing MRP models.
#
# JAX must match the local CUDA install. On Skidrow (RTX 4070 SUPER) install
# the CUDA build explicitly; the plain `jax` wheel is CPU-only:
#
#   pip install --upgrade "jax[cuda12]"
#
# Verify before fitting:
#   python -c "import jax; print(jax.devices())"   # -> [CudaDevice(id=0)]

jax[cuda12]>=0.8
numpyro>=0.21

# bambi provides the formula layer; it pulls in pymc. The model is fit through
# numpyro's NUTS, so pymc is not doing the sampling. Dropping bambi in favour of
# a hand-written NumPyro model is an open decision -- see CLAUDE.md.
bambi>=0.17
arviz>=0.23

pandas>=2.2
numpy>=2.1

# Reads the ACS poststratification frame (.rds) produced by the R pipeline.
pyreadr>=0.5
